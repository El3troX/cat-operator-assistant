# Copy to frontend/.env.local to override. Vite inlines VITE_* values into the
# browser bundle, so never put secrets here.

# Backend base URL. Default: http://localhost:8000
# VITE_API_BASE_URL=http://localhost:8000
